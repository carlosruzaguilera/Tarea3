// analysis.c
#include "analysis.h"
#include <stdio.h>
#include <math.h>

void calculateMostFrequentWords(Document *doc) {
    // Implementa una función de recorrido en el BST doc->wordTree para encontrar y mostrar las 10 palabras más frecuentes
    // Esta función podría necesitar una función auxiliar recursiva y una estructura de datos adicional para mantener un registro de las palabras más frecuentes
}

void calculateMostRelevantWords(BST *docTree, Document *doc) {
    // Implementa una función de recorrido en el BST doc->wordTree para calcular el TF-IDF de cada palabra
    // También recorre el BST docTree para obtener el número total de documentos y el número de documentos que contienen cada palabra
    // Esta función podría necesitar una función auxiliar recursiva y una estructura de datos adicional para mantener un registro de las palabras más relevantes
}

